"""
Reconstructor implementations.

Each method exposes a small, uniform surface (fit / forward / reconstruct)
so experiment code can loop over method IDs without method-specific branches
beyond construction.
"""
